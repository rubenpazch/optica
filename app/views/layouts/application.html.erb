<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Óptica - Sistema de Gestión" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Optica">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      <!-- Authenticated Layout with Sidebar -->
      <nav class="navbar">
        <div class="nav-container">
          <%= link_to "Óptica", root_path, class: "nav-brand" %>
          
          <div class="nav-links">
            <span class="nav-user">Hola, <%= current_user.email %></span>
            <%= button_to "Cerrar Sesión", destroy_user_session_path, method: :delete, class: "nav-link logout-btn" %>
          </div>
        </div>
      </nav>

      <div class="app-layout">
        <!-- Sidebar -->
        <aside class="sidebar">
          <div class="sidebar-header">
            <h3 class="sidebar-title">Panel de Control</h3>
            <p class="sidebar-subtitle">¡Bienvenido de nuevo!</p>
          </div>
          
          <nav class="sidebar-nav">
            <%= link_to root_path, class: "sidebar-nav-link #{'active' if current_page?(root_path)}" do %>
              <span class="sidebar-nav-icon">🏠</span>
              <span>Inicio</span>
            <% end %>
            
            <%= link_to edit_user_registration_path, class: "sidebar-nav-link #{'active' if current_page?(edit_user_registration_path)}" do %>
              <span class="sidebar-nav-icon">👤</span>
              <span>Perfil</span>
            <% end %>
            
            <%= link_to "#", class: "sidebar-nav-link" do %>
              <span class="sidebar-nav-icon">📊</span>
              <span>Estadísticas</span>
            <% end %>
            
            <%= link_to "#", class: "sidebar-nav-link" do %>
              <span class="sidebar-nav-icon">⚙️</span>
              <span>Configuración</span>
            <% end %>
            
            <%= link_to "#", class: "sidebar-nav-link" do %>
              <span class="sidebar-nav-icon">📞</span>
              <span>Soporte</span>
            <% end %>
          </nav>
        </aside>

        <main class="main-content-with-sidebar">
          <% if notice %>
            <div class="alert alert-success">
              <%= notice %>
            </div>
          <% end %>

          <% if alert %>
            <div class="alert alert-danger">
              <%= alert %>
            </div>
          <% end %>

          <%= yield %>
        </main>
      </div>
    <% else %>
      <!-- Authentication Layout (no sidebar) -->
      <div class="auth-page">
        <%= yield %>
      </div>
    <% end %>
  </body>
</html>
