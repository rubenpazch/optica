<div class="profile-container">
  <div class="profile-form">
    <div class="profile-header">
      <h2>Editar Perfil</h2>
      <p class="profile-subtitle">Actualiza la información de tu cuenta</p>
    </div>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: "profile-form-content" }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="form-section">
        <h3>Información de la Cuenta</h3>
        
        <div class="form-group">
          <%= f.label :email, "Correo Electrónico", class: "form-label" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: "Ingresa tu correo electrónico" %>
        </div>

        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div class="alert alert-info">
            <strong>Confirmación pendiente:</strong> Estamos esperando que confirmes <%= resource.unconfirmed_email %>
          </div>
        <% end %>
      </div>

      <div class="form-section">
        <h3>Cambiar Contraseña</h3>
        <p class="form-section-subtitle">Deja en blanco si no quieres cambiar tu contraseña</p>
        
        <div class="form-group">
          <%= f.label :password, "Nueva Contraseña", class: "form-label" %>
          <%= f.password_field :password, autocomplete: "new-password", class: "form-control", placeholder: "Ingresa nueva contraseña" %>
          <% if @minimum_password_length %>
            <small class="form-text">Mínimo <%= @minimum_password_length %> caracteres requeridos</small>
          <% end %>
        </div>

        <div class="form-group">
          <%= f.label :password_confirmation, "Confirmar Nueva Contraseña", class: "form-label" %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control", placeholder: "Confirma nueva contraseña" %>
        </div>

        <div class="form-group">
          <%= f.label :current_password, "Contraseña Actual", class: "form-label" %>
          <%= f.password_field :current_password, autocomplete: "current-password", class: "form-control", placeholder: "Ingresa contraseña actual" %>
          <small class="form-text">Necesitamos tu contraseña actual para confirmar los cambios</small>
        </div>
      </div>

      <div class="form-actions">
        <%= f.submit "Actualizar Perfil", class: "btn btn-primary btn-block" %>
      </div>
    <% end %>

    <div class="danger-zone">
      <h3>Zona de Peligro</h3>
      <p>Una vez que elimines tu cuenta, no hay vuelta atrás. Por favor, asegúrate.</p>
      
      <div class="danger-actions">
        <%= button_to "Eliminar Cuenta", registration_path(resource_name), 
            data: { 
              confirm: "¿Estás seguro de que quieres eliminar tu cuenta? Esta acción no se puede deshacer.", 
              turbo_confirm: "¿Estás seguro de que quieres eliminar tu cuenta? Esta acción no se puede deshacer." 
            }, 
            method: :delete, 
            class: "btn btn-danger" %>
      </div>
    </div>

    <div class="profile-footer">
      <%= link_to "← Volver al Panel", root_path, class: "btn btn-secondary" %>
    </div>
  </div>
</div>
